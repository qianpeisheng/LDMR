# Pseudo Label Tooling (ScanNet-Developed)

Standalone helper scripts for inspecting and regenerating pseudo labels. They are
not needed for the training pipeline, which regenerates pseudo labels per stage.

**Important:**
- These scripts were developed/validated primarily for **ScanNet** incremental workflows.
- Run them from the repo root so relative paths like `configs/...` and `data/...` resolve correctly.

